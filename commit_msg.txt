feat: stages 1-4 implementation + audit fixes

Backend:
- Auth/Users/Points/Recharge/Games/GameCore/Download modules
- Fix: withdrawal (DOWN) approve correctly marks order as APPROVED
- Fix: freeze idempotency key now uses stable order.id
- Fix: BET/WIN route through platform account for fund conservation
- Fix: CommissionService wired via AgentsModule; rebate 0.5% per round
- Fix: strip weight from payTable in /api/games to hide win probabilities
- Seed: admin account (admin/Admin@123456), 3 games, platform balance

Frontend:
- Vue 3 + Pinia + vue-router; lobby/recharge/flow/lucky-wheel views
- Fix: App.vue restores session via /users/me on startup (no re-login on refresh)
- Fix: lobby fetches real /api/games; countdown uses reactive object + setInterval
